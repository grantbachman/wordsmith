<%= form_for @word do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<br />
	<%= f.label "Word:" %>
	<%= f.text_field :name %>	
	<%= f.submit "Add" %>
<% end %>

<% @words.each do |word| %>
	<%= word.name %><br />
<% end %>
